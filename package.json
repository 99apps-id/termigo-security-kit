{
  "name": "termigo-security-kit",
  "private": true,
  "version": "0.1.0",
  "description": "Static security, dependency audit, and malware scanning tools for Termigo.",
  "type": "module",
  "scripts": {
    "build": "esbuild src/index.js --bundle --format=esm --platform=browser --outfile=dist/main.js",
    "check": "node --check dist/main.js",
    "test": "node test/scope.mjs",
    "zip": "node scripts/zip.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  },
  "allowScripts": {
    "esbuild@0.24.2": true
  }
}
